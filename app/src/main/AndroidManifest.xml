<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muratoter.isg.raporla">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ThemeNoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".IsgUzmaniActivity" />
        <activity android:name=".UretimSorumlusuActivity" />
        <activity android:name=".IsgUzmaniAraActivity" />
        <activity android:name=".RaporOlusturActivity" />
        <activity android:name=".AyrintiliRaporActivity"></activity>
    </application>

</manifest>